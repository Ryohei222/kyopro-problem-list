export const publicRoutes: string[] = ["/", "/problemset/[id]"];

export const authRoutes: string[] = ["/sign-up", "/sign-in"];

export const apiAuthPrefix: string = "/api/auth/signin";

export const DEFAULT_LOGIN_REDIRECT: string = "/";
